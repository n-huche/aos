# {{Título}}

**Fase:** [{{PREFIXO}}-{{nn}}](../../../../../../projects/{status}/{slug}/phases/{prefixo}-{nn}-{slug-fase}.md)  
**Prazo:** YYYY-MM-DD | não definido | none  
**Status:** pending | completed | recurring

## O quê

1. _passo extraído da fase_
2. _…

## Por quê

- _motivo já decidido na fase (ou nesta nota, se independente)_

## Quando

- _datas / horários; se recurring: cadência + até quando ou none_

## Onde, se relevante

- _local_

## Notas, opcional

_Detalhes, opções, contatos — só o necessário._

---

Obs: **Fase:** só em tarefa de fase (link relativo). Independente: omitir o campo. `{status}` = pasta do projeto (`pending` | `in-progress` | `paused` | `completed`). Em `pending/{prazo}/project-tasks/…` o link usa `../../../../../../projects/{status}/…` (6 `../`). Em `completed/` e `recurring/` (sem `{prazo}/`): `../../../../../projects/{status}/…` (5 `../`). **Prazo:** de fase = copiar o da fase; independente `pending` = `não definido` se o usuário não der data. `recurring` sem fim = `none`. **Quando** não vira Prazo. Não copiar heading vazio. Tarefa de fase **não traz nada de novo** — compacta Entrega + Como da fase. Se a fase não disser, não entra aqui.

Índice [tasks](../../tasks/tasks.md): só o `##` que tiver item; sem `--`. Ordem: Recorrentes · Atrasadas · Hoje · Três dias · Uma semana · Um mês · Longo prazo · Sem prazo. Se a seção não existir, inserir o heading nessa ordem; se sair o último item, apagar o heading. `completed` não entra.
